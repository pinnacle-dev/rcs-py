# This file was auto-generated by Fern from our API Definition.

import typing

from ...core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ...core.request_options import RequestOptions
from ...types.e_164_phone_number import E164PhoneNumber
from ...types.sim_swap_response import SimSwapResponse
from .raw_client import AsyncRawSimSwapClient, RawSimSwapClient

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class SimSwapClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawSimSwapClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawSimSwapClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawSimSwapClient
        """
        return self._raw_client

    def check(
        self,
        *,
        phone_number: E164PhoneNumber,
        period_hours: typing.Optional[int] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SimSwapResponse:
        """
        Check whether a phone number's SIM was changed during a recent lookback window. Use this to flag account takeover risk before sign-in, password reset, payment, or other sensitive actions.

        <Note>
          Limited availability. Contact [founders@pinnacle.sh](mailto:founders@pinnacle.sh) to request access.
        </Note>

        Parameters
        ----------
        phone_number : E164PhoneNumber

        period_hours : typing.Optional[int]
            Lookback window, in hours, for detecting a recent SIM change. Must be from 1 to 2400. Defaults to 240.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SimSwapResponse
            Returns SIM swap risk for the requested phone number.

        Examples
        --------
        from rcs import Pinnacle

        client = Pinnacle(
            api_key="YOUR_API_KEY",
        )
        client.network.sim_swap.check(
            phone_number="+14155550123",
            period_hours=240,
        )
        """
        _response = self._raw_client.check(
            phone_number=phone_number, period_hours=period_hours, request_options=request_options
        )
        return _response.data


class AsyncSimSwapClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawSimSwapClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawSimSwapClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawSimSwapClient
        """
        return self._raw_client

    async def check(
        self,
        *,
        phone_number: E164PhoneNumber,
        period_hours: typing.Optional[int] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SimSwapResponse:
        """
        Check whether a phone number's SIM was changed during a recent lookback window. Use this to flag account takeover risk before sign-in, password reset, payment, or other sensitive actions.

        <Note>
          Limited availability. Contact [founders@pinnacle.sh](mailto:founders@pinnacle.sh) to request access.
        </Note>

        Parameters
        ----------
        phone_number : E164PhoneNumber

        period_hours : typing.Optional[int]
            Lookback window, in hours, for detecting a recent SIM change. Must be from 1 to 2400. Defaults to 240.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SimSwapResponse
            Returns SIM swap risk for the requested phone number.

        Examples
        --------
        import asyncio

        from rcs import AsyncPinnacle

        client = AsyncPinnacle(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.network.sim_swap.check(
                phone_number="+14155550123",
                period_hours=240,
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.check(
            phone_number=phone_number, period_hours=period_hours, request_options=request_options
        )
        return _response.data
