# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from .form_background import FormBackground
from .form_theme_override_colors import FormThemeOverrideColors
from .form_theme_override_content_alignment import FormThemeOverrideContentAlignment
from .form_theme_override_corner_radius import FormThemeOverrideCornerRadius
from .form_theme_override_font_family import FormThemeOverrideFontFamily
from .form_theme_override_theme_mode import FormThemeOverrideThemeMode


class FormThemeOverride(UniversalBaseModel):
    """
    Sparse override layered on top of your team's theme. Every field is optional; provided fields replace the corresponding team defaults at render time. URL fields must use `https://`.
    """

    logo_url: typing.Optional[str] = pydantic.Field(default=None)
    """
    Logo displayed at the top of the form. Square images render as an inline mark; wide landscape images render as a full-width banner.
    """

    favicon_url: typing.Optional[str] = pydantic.Field(default=None)
    """
    Favicon shown in the browser tab when the form page loads.
    """

    theme_mode: typing.Optional[FormThemeOverrideThemeMode] = pydantic.Field(default=None)
    """
    Rendering mode. `light` forces the light palette, `dark` forces dark, and `auto` follows the respondent's OS-level appearance setting.
    """

    colors: typing.Optional[FormThemeOverrideColors] = pydantic.Field(default=None)
    """
    Per-mode palette overrides. Each mode is an object with optional `primary`, `background`, and `text` hex colors. Omit a mode to inherit the team default for it.
    """

    font_family: typing.Optional[FormThemeOverrideFontFamily] = pydantic.Field(default=None)
    """
    Typeface stack. `system` uses the respondent's OS default sans-serif.
    """

    corner_radius: typing.Optional[FormThemeOverrideCornerRadius] = pydantic.Field(default=None)
    """
    Border-radius preset for cards, inputs, and buttons.
    """

    content_alignment: typing.Optional[FormThemeOverrideContentAlignment] = pydantic.Field(default=None)
    """
    Horizontal alignment for the form header (title, description, logo).
    """

    background: typing.Optional[FormBackground] = pydantic.Field(default=None)
    """
    Background layer for the page. Choose solid, gradient, image, or a built-in pattern.
    """

    submit_button_label: typing.Optional[str] = pydantic.Field(default=None)
    """
    Text shown on the submit button. Defaults to `Submit`.
    """

    success_message: typing.Optional[str] = pydantic.Field(default=None)
    """
    Message shown on the success panel after a successful submit (unless `redirect_url` is set, in which case the respondent is redirected).
    """

    redirect_url: typing.Optional[str] = pydantic.Field(default=None)
    """
    When set, the submitter is redirected here after a successful submit instead of seeing the success panel.
    """

    privacy_url: typing.Optional[str] = pydantic.Field(default=None)
    """
    Link target for the `Privacy` footer link. Omit or set to null to hide the link.
    """

    terms_url: typing.Optional[str] = pydantic.Field(default=None)
    """
    Link target for the `Terms` footer link. Omit or set to null to hide the link.
    """

    og_image_url: typing.Optional[str] = pydantic.Field(default=None)
    """
    Image shown when the form URL is shared in social / messaging link previews (Open Graph).
    """

    og_description: typing.Optional[str] = pydantic.Field(default=None)
    """
    Open Graph description used in link previews. Falls back to the form's `description` when unset.
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
