# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from .form_submission_event_conversation import FormSubmissionEventConversation
from .form_submission_event_form import FormSubmissionEventForm
from .form_submission_event_submission import FormSubmissionEventSubmission


class FormSubmissionEvent(UniversalBaseModel):
    """
    Webhook body delivered to subscribers of `FORM.SUBMISSION` after a recipient completes a form. Verify the `PINNACLE-SIGNING-SECRET` header matches your webhook's signing secret before trusting the payload.
    """

    type: typing.Literal["FORM.SUBMISSION"] = "FORM.SUBMISSION"
    sender: str = pydantic.Field()
    """
    Top-level sender identifier — same value as `submission.from`. Exposed at the event root so you can route on it without digging into the submission object.
    """

    conversation: typing.Optional[FormSubmissionEventConversation] = None
    form: FormSubmissionEventForm
    submission: FormSubmissionEventSubmission

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
